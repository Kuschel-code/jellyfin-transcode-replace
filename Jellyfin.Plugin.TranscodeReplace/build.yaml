---
name: "Transcode & Replace"
guid: "eff73e61-0320-4bce-bb2c-27fb68604e9d"
version: "0.0.2.0"
targetAbi: "10.11.0.0"
framework: "net9.0"
owner: "Kuschel-code"
overview: "Batch-transcode library media to HEVC/AV1 and atomically replace the original."
description: >
  Discovers library media, transcodes it to a target codec using detected
  hardware or software encoders, verifies the result and atomically replaces
  the original file. Includes a persistent job queue, dry-run mode, backup
  policy, HDR preservation and an optional VMAF gate.
category: "General"
artifacts:
  - "Jellyfin.Plugin.TranscodeReplace.dll"
changelog: >
  0.0.2.0 - Redesigned settings page (grouped cards, Settings/History tabs) and a
  History view that shows each processed file with its poster, codec change and size
  saving, like a transcode log. 0.0.1.0 - First release with the full pipeline.
